# Build output
/pbmac
/pbmac.exe
/pbmac-darwin-arm64
/pbmac-darwin-arm64.sha256
*.test

# CI / local smoke-test scratch
/smoke/

# macOS GUI (XcodeGen-generated project + Xcode build state)
/macos/PBMac.xcodeproj/
/macos/*.xcworkspace/
/macos/DerivedData/
/macos/build/
.DS_Store
xcuserdata/

# Local config / secrets
*.key
key.json

# Local credentials env file (real creds live here; never commit — see .env.example)
.env
.env.*
!.env.example
